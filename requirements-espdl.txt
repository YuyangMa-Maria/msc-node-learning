# Tested in a separate Python 3.10 environment for ESP-DL conversion.
# This include makes the file sufficient for a clean conversion environment.
-r requirements.txt
# esp-ppq 1.3.6 and the tested ONNX Runtime path pre-date NumPy 2.x.
numpy>=1.26,<2
esp-ppq==1.3.6
onnx==1.17.0
onnxsim==0.4.36
protobuf==3.20.2
